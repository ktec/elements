<div class="gallery">
  <h1 class="title"><%=h gallery.title %></h1>
  <h2 class="decsription"><%=h gallery.description %></h2>
  <% gallery.pictures.each do |element| %>
  <%= render element.attachable if element.attachable.is_a?(Picture) %>
  <% end %>
</div>