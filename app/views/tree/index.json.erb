[<% @elements.each_with_index do |element,i| %>
{
  "attr":
    {
      "id":"node_<%= element.id %>",
      <% if element.attachable.nil? %>
      "rel":"<%= element.is_root? ? "drive" : !element.type.nil? ? element.type : element.has_children? ? "folder" : "folder" %>"
      <% else %>
      "rel":"<%= element.attachable.type %>"
      <% end %>
    },
  "data":"<%=h element.name %>",
  "state":"closed"
}<%= "," if i < @elements.count-1 %>
<% end %>]

