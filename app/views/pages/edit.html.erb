<h1>Editing page</h1>

<% semantic_form_for(@page, :html => {:multipart => true, 'data-remote' => true}) do |f| %>
  <%= render "form", :f => f %>
  <%= f.buttons %>
<% end %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>