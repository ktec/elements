<h1>New picture</h1>

<% form_for(@picture, :html => {:multipart => true}) do |f| %>
  <%= f.error_messages %>

  <%= render "elements/fields", :f => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', pictures_path %>