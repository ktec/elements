[<% @elements.each_with_index do |element,i| %>
{
  "attr":
    {
      "id":"node_<%= element.id %>",
      "rel":"<%= element.is_root? ? "drive" : element.has_children? ? "folder" : "folder" %>"
    },
  "data":"<%=h element.name %>",
  "state":"closed"
}<%= "," if i < @elements.count-1 %>
<% end %>]

