<h1>New element</h1>

<% form_for(@element) do |f| %>
  <%= f.error_messages %>
  <%= render "form", :f => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', elements_path %>