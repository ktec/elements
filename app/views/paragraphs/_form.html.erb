<% f.inputs do %>
  <%= f.input :attachable_type, :as => :hidden, :value => f.object.class.to_s unless f.object.is_a?(Paragraph) %>
  <%= f.input :content, :as => :textile, :id => ("element_attachable_attributes_content"  unless f.object.is_a?(Paragraph)) %>
<% end %>

