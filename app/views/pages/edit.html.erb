<h1>Editing page</h1>

<% form_for(@page, :html => {:multipart => true}) do |f| %>
  <%= f.error_messages %>

  <%= render "form", :f => f %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>